# Release Notes

## v1.0.0 - Documentation & Cleanup Update

This release focuses on stabilizing the project structure, improving documentation, and cleaning up legacy assets.

### 🚀 Features & Improvements
*   **Documentation:** Complete rewrite of `README.md` to clearly explain the Client-Server architecture and setup instructions.
*   **Licensing:** Added standard **MIT License** to open-source the project.
*   **Registry Integration:** Added `add_run_wallpaper.reg.txt` template to easily add a "Run WebBasedWallpaper" option to the Windows desktop context menu.

### 🐛 Bug Fixes
*   **API URL:** Fixed a typo in the API endpoint URL in `webBasedWallPaper.ps1` (`//todos` -> `/todos`).

### 🧹 Maintenance
*   **Cleanup:** Removed unused assets (`1.png`, `2.png`, `2_edit.png`) and obsolete scripts (`SetWallpaperWithText.ps1`, `multiTodoOnImge.ps1`) to keep the repository clean.
